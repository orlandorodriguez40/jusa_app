name: login_app
description: "App de login y panel para tomar y enviar fotos por asignación."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.7
  logger: ^2.0.2
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.2.3
  photo_view: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/images/logo-jusa-2-opt.png
